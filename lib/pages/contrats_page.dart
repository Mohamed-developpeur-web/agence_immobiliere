import 'package:flutter/material.dart';

class ContratsPage extends StatelessWidget {
  const ContratsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Gestion des contrats"),
      ),
    );
  }
}
